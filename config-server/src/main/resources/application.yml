server:
  port: 8888

spring:
  application:
    name: config-server
  # profiles:
  #   active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/youssef-abd/jee-config-repo.git
          default-label: main
          timeout: 4
        # native:
        #   app-search-locations: file:///C:/JEE_micro/config-repo
